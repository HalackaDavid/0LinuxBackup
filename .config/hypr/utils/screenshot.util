#!/bin/zsh

screenshotPath="$NCORMediaPath/Screenshots"

case $1 in
    "1")
        grim "$screenshotPath/$(date)_1.png"
        ;;

    "2")
        grim -g "$(slurp)" "$screenshotPath/$(date)_2.png"
        ;;
esac

unset screenshotPath
